---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<span class="brand-name">DzwS</span>
		<nav class="footer-nav">
			<a href="/">首页</a>
			<a href="/blog">博客</a>
			<a href="/about">关于</a>
			<a href="https://github.com/DzwS" target="_blank">GitHub</a>
		</nav>
		<span class="copyright">&copy; {today.getFullYear()} · Powered by <a href="https://astro.build/" target="_blank">Astro</a></span>
	</div>
</footer>

<style>
	footer {
		background: #fff;
		color: var(--gray);
		margin-top: 4rem;
		border-top: 1px solid rgba(0, 0, 0, 0.06);
	}

	.footer-content {
		max-width: 960px;
		margin: 0 auto;
		padding: 1.5rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 2rem;
	}

	.brand-name {
		font-size: 1.1rem;
		font-weight: 700;
		color: var(--black);
		flex-shrink: 0;
	}

	.footer-nav {
		display: flex;
		gap: 1.5rem;
	}

	.footer-nav a {
		color: var(--gray);
		text-decoration: none;
		font-size: 0.9rem;
		transition: color 0.2s ease;
	}

	.footer-nav a:hover {
		color: var(--black);
	}

	.copyright {
		font-size: 0.85rem;
		color: var(--gray);
		flex-shrink: 0;
	}

	.copyright a {
		color: var(--accent, #8b5cf6);
		text-decoration: none;
	}

	.copyright a:hover {
		text-decoration: underline;
	}

	@media (max-width: 768px) {
		.footer-content {
			flex-direction: column;
			gap: 1rem;
			text-align: center;
		}

		.footer-nav {
			flex-wrap: wrap;
			justify-content: center;
			gap: 1rem;
		}
	}
</style>
